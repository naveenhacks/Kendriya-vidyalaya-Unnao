// Firebase has been completely removed from this application.
// This file exists only to prevent import errors in any legacy code that might still reference it.
// The application runs in a pure client-side mode using localStorage.

export const auth = {};
export const db = {};
export const storage = {};